<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Appointment</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <%- include("partials/navbar", { activePage: "appointments" }) %>
  <div class="container">
    <div class="page-header">
      <h2>Edit Appointment</h2>
      <a href="/appointments" class="btn btn-secondary">Back</a>
    </div>

    <div class="form-container">
      <form method="POST" action="/appointments/<%= appointment._id %>/edit" class="form">
        <div class="form-group">
          <label for="date">Date</label>
          <input type="date" id="date" name="date" value="<%= new Date(appointment.date).toISOString().slice(0,10) %>" required>
        </div>

        <div class="form-group">
          <label for="summary">Summary</label>
          <input type="text" id="summary" name="summary" value="<%= appointment.summary || '' %>" required>
        </div>

        <div class="form-group">
          <label for="notes">Notes</label>
          <textarea id="notes" name="notes" rows="5"><%= appointment.notes || '' %></textarea>
        </div>

        <div class="form-actions">
          <button type="submit" class="btn btn-primary">Save</button>
          <a href="/appointments" class="btn btn-secondary">Cancel</a>
        </div>
      </form>
    </div>
  </div>
  <footer>
    <p>&copy; 2025 Medical Database Management System</p>
  </footer>
</body>
</html>
