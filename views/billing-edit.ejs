<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Billing</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <%- include("partials/navbar", { activePage: "billings" }) %>
  <div class="container">
    <div class="page-header">
      <h2>Edit Billing</h2>
      <a href="/billings" class="btn btn-secondary">Back</a>
    </div>

    <div class="form-container">
      <form method="POST" action="/billings/<%= billing._id %>/edit" class="form">
        <div class="form-group">
          <label for="amount">Amount</label>
          <input type="number" step="0.01" id="amount" name="amount" value="<%= billing.amount %>" required>
        </div>

        <div class="form-group">
          <label for="status">Status</label>
          <select id="status" name="status">
            <option value="Paid" <%= billing.status === "Paid" ? "selected" : "" %>>Paid</option>
            <option value="Pending" <%= billing.status === "Pending" ? "selected" : "" %>>Pending</option>
            <option value="Due" <%= billing.status === "Due" ? "selected" : "" %>>Due</option>
          </select>
        </div>

        <div class="form-group">
          <label for="paymentDate">Payment Date</label>
          <input type="date" id="paymentDate" name="paymentDate" value="<%= billing.paymentDate ? new Date(billing.paymentDate).toISOString().slice(0,10) : '' %>">
        </div>

        <div class="form-actions">
          <button type="submit" class="btn btn-primary">Save</button>
          <a href="/billings" class="btn btn-secondary">Cancel</a>
        </div>
      </form>
    </div>
  </div>

  <footer>
    <p>&copy; 2025 Medical Database Management System</p>
  </footer>
</body>
</html>
