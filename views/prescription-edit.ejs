<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Prescription</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <%- include("partials/navbar", { activePage: "prescriptions" }) %>

  <div class="container">
    <div class="page-header">
      <h2>Edit Prescription</h2>
      <a href="/prescriptions" class="btn btn-secondary">Back</a>
    </div>

    <div class="form-container">
      <form method="POST" action="/prescriptions/<%= rx._id %>/edit" class="form">
        <div class="form-group">
          <label for="medicationName">Medication</label>
          <input type="text" id="medicationName" name="medicationName" value="<%= rx.medicationName %>" required>
        </div>

        <div class="form-group">
          <label for="dosage">Dosage</label>
          <input type="text" id="dosage" name="dosage" value="<%= rx.dosage %>">
        </div>

        <div class="form-group">
          <label for="frequency">Frequency</label>
          <input type="text" id="frequency" name="frequency" value="<%= rx.frequency %>">
        </div>

        <div class="form-group">
          <label for="instructions">Instructions</label>
          <textarea id="instructions" name="instructions" rows="3"><%= rx.instructions %></textarea>
        </div>

        <div class="form-group">
          <label for="type">Type</label>
          <input type="text" id="type" name="type" value="<%= rx.type || '' %>">
        </div>

        <div class="form-group">
          <label for="status">Status</label>
          <select id="status" name="status">
            <option value="active" <%= rx.status === "active" ? "selected" : "" %>>Active</option>
            <option value="completed" <%= rx.status === "completed" ? "selected" : "" %>>Completed</option>
          </select>
        </div>

        <div class="form-group">
          <label for="startDate">Start Date</label>
          <input type="date" id="startDate" name="startDate" value="<%= rx.startDate ? new Date(rx.startDate).toISOString().slice(0,10) : '' %>">
        </div>

        <div class="form-group">
          <label for="endDate">End Date</label>
          <input type="date" id="endDate" name="endDate" value="<%= rx.endDate ? new Date(rx.endDate).toISOString().slice(0,10) : '' %>">
        </div>

        <div class="form-actions">
          <button type="submit" class="btn btn-primary">Save</button>
          <a href="/prescriptions" class="btn btn-secondary">Cancel</a>
        </div>
      </form>
    </div>
  </div>

  <footer>
    <p>&copy; 2025 Medical Database Management System</p>
  </footer>
</body>
</html>
